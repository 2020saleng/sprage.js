export function compilerData(vm){
    
    const TypeFunMap={}
    
}


function compiler(content) {
  const type = Object.prototype.toString.call(content);
  const TypeFunMap = {
    //    ' [object String]':
  };
}
